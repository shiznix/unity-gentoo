diff -Nur d/src/gcal-source-dialog.c e/src/gcal-source-dialog.c
--- d/src/gcal-source-dialog.c	2018-11-03 00:24:04.000000000 +0100
+++ e/src/gcal-source-dialog.c	2019-11-12 11:26:10.622823486 +0100
@@ -691,6 +691,52 @@
   return g_variant_new_tuple (params2, 3);
 }
 
+static gboolean
+in_desktop (const gchar *name)
+{
+	const gchar *desktop_name_list;
+	gchar **names;
+	gboolean in_list = FALSE;
+	gint i;
+
+	desktop_name_list = g_getenv ("XDG_CURRENT_DESKTOP");
+	if (!desktop_name_list)
+		return FALSE;
+
+	names = g_strsplit (desktop_name_list, ":", -1);
+	for (i = 0; names[i] && !in_list; i++)
+		if (strcmp (names[i], name) == 0) {
+			in_list = TRUE;
+			break;
+		}
+	g_strfreev (names);
+
+	return in_list;
+}
+
+static void
+spawn_uoa (const gchar *action,
+           const gchar *arg)
+{
+  const gchar *cc_uoa = "unity-control-center online-accounts";
+  gchar *command;
+  GError *error = NULL;
+
+  if (action && arg)
+    command = g_strconcat (cc_uoa, " ", action, " ", arg, NULL);
+  else
+    command = g_strconcat (cc_uoa, NULL);
+
+  g_spawn_command_line_async (command, &error);
+  g_free(command);
+
+  if (error != NULL)
+  {
+    g_warning ("Couldn't open Online Accounts panel: %s", error->message);
+    g_error_free (error);
+  }
+}
+
 static void
 spawn_goa_with_args (const gchar *action,
                      const gchar *arg)
@@ -712,7 +758,9 @@
       return;
     }
 
-  g_dbus_proxy_call_sync (proxy,
+  GVariant *variant;
+
+  variant = g_dbus_proxy_call_sync (proxy,
                           "Activate",
                           build_dbus_parameters (action, arg),
                           G_DBUS_CALL_FLAGS_NONE,
@@ -720,6 +768,13 @@
                           NULL,
                           NULL);
 
+  if (!variant && in_desktop("Unity"))
+    {
+      spawn_uoa (action, arg);
+    }
+
+  g_variant_unref (variant);
+
   g_clear_object (&proxy);
 }
 
