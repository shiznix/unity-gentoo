* USE="doc debug mono tests"
* Audit DEPEND and RDEPEND lists
* Convert all ebuilds to EAPI=5 with subslot rebuilds where necessary
* Convert ebuilds to use new python-r1.eclass for multi-abi installs (see dev-libs/dee)
* Convert ebuilds to use new distutils-r1.eclass
