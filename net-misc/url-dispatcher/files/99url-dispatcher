# This file is sourced by Xsession(5), not executed.
# Start 'url-dispatcher' if we are in a Unity desktop session

if [ "x$DESKTOP_SESSION" = "xunity" ]; then
	exec /usr/libexec/url-dispatcher/url-dispatcher &
fi
